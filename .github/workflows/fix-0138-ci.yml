name: Fix 0.13.8 CI contracts

permissions:
  contents: write

on:
  pull_request:
    branches:
      - main

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/attention-auto-mail-handling-0.13.8
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply fixes
        run: python tools/fix_0138_ci.py
      - name: Commit fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix: preserve backlog semantics and attention deep links"
          git push origin HEAD:feat/attention-auto-mail-handling-0.13.8
